spring:
  application:
    name: quickstart-bootstrap
  profiles:
    active: dev
server:
  servlet:
    context-path: "/api"
  port: 8080
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
